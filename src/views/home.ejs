<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>App 版本管理系统</title>
  <style>
    html,
    body {
      margin: 0;
      padding: 0;
      font-size: 16px;
      width: 100vw;
      height: 100vh;
    }
    body {
      display: flex;
      flex-direction: column;
    }
    .header {
      width: 100vw;
      height: 6rem;
      padding: 0 2rem;
      background: bisque;
      box-sizing: border-box;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .content {
      flex: 1;
      width: 100vw;
    }
    .tabs > a {
      margin-right: .5rem;
    }
  </style>
</head>
<body>
  <header class="header">
    <h2>App 版本管理系统</h2>
    <section style="width: 15rem;display: flex;justify-content: space-between;">
      <span class="tabs">
        <a href="/?tab=new">新建</a><a href="/?tab=list">管理</a>
      </span>
      <span>
        <span><span>用户名：</span><%= userName %></span>
      </span>
    </section>
  </header>
  <div class="content">
    <% if (query.tab === 'new') { %>
      <!-- 新建页面 -->
      <%- include('tabs/new') %>
    <% } else if (query.tab === 'list') { %>
      <!-- 列表页面 -->
      <%- include('tabs/list', { list }) %>
    <% } else { %>
      <script>window.location.href = '/?tab=new'</script>
    <% } %>
  </div>
</body>
</html>